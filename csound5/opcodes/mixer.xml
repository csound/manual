<chapter id="MixerOpcodes">
  <title>Mixer Opcodes</title>

  <para>
    The Mixer family of opcodes provides a global mixer for Csound. The Mixer opcodes include 

    <link linkend="MixerSend"><citetitle>MixerSend</citetitle></link> for
    sending (that is, mixing in) an arate signal from any instrument to a channel of a mixer buss,

    <link linkend="MixerReceive"><citetitle>MixerReceive</citetitle></link> for
    receiving an arate signal from a channel of any mixer buss in any instrument,

    <link linkend="MixerSetLevel"><citetitle>MixerSetLevel</citetitle></link> (krate) and 
    <link linkend="MixerSetLevel_i"><citetitle>MixerSetLevel_i</citetitle></link> (irate) for
    controlling the level of the signal sent from a particular send to a particular buss,

    <link linkend="MixerGetLevel"><citetitle>MixerGetLevel</citetitle></link> for
    reading (at krate) the level for sending a signal from a particular send to a particular buss, and

    <link linkend="MixerClear"><citetitle>MixerClear</citetitle></link> for resetting the busses to zero before the next kperiod of a performance.
    </para>

</chapter>
